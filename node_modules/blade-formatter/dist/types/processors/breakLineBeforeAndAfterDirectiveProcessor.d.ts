import { Processor } from "./processor";
export declare class BreakLineBeforeAndAfterDirectiveProcessor extends Processor {
    private bladeComments;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    breakLineBeforeAndAfterDirective(content: string): string;
}
